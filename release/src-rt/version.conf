KERNEL_VER=3.0
FS_VER=0.6
KERNEL_VER_AM=$(subst .,,$(KERNEL_VER))
FS_VER_AM=$(subst .,,$(FS_VER))

SERIALNO=102.8
EXTENDNO=beta3-dav20

ifeq ($(ROG_UI),y)
EXTENDNO:=$(EXTENDNO)_rog
endif
# SLIM flavor (CTRLD=y from the CI matrix): the Control-D / slimmed build.
ifeq ($(CTRLD),y)
EXTENDNO:=$(EXTENDNO)-ctrld
endif
RCNO=0
